spring:
  profiles.active: local
  application:
     name: quotes-service
security:
  basic:
    authorize-mode: none
info:
  build:
    group: ${group}
    name: ${name}
    description: ${description}
    version: ${version}